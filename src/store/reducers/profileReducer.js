const profileReducer = (state = {}, action) => {
  switch (action.type) {
    case 'ADD_PROFILE':
      return {
        ...state
      }
    default:
      return {
        ...state
      }
  }
}

export default profileReducer
